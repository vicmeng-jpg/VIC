---
layout: default
title: "重構之戰：將爬蟲腳本升格為專業軟體"
parent: "我的學習日誌"
nav_order: -20260411.2
---

# 🚀 學習日誌：從腳本到軟體的重構之路 (The Refactoring Journey)

**日期**：2026年4月11日 凌晨

當我的「船員評估試題批次爬蟲」功能已經達到趨近完美、且成功學習使用 Git 推播上 GitHub 之後，我深刻意識到，是時候從「寫語法」的腳本小子（Scripter）畢業，前往「寫架構」的軟體工程師（Software Engineer）境界了。

面對目前高達 386 行、所有變數與邏輯混雜的單體檔案 `crawler.py`，我決定對它動一場大手術。這不是為了增加新功能，而是要重塑它的體質。為此，我擬定了「四大重構階段藍圖」，準備以業界的工業標準來翻新它：

## 🛠️ 第一階段：模組化革命 (The Breakup)
**目標：解決程式過胖問題，實現「職責分離」。**
*   **50 行原則**：打破巨石，強迫把每個函式壓在 20 行內，每個檔案壓在 100 行內。
*   **架構拆分**：建立 `config.py` 來收容所有寫死的 URL 與設定變數；建立 `utils.py` 來管理「純邏輯工具」（如字串正規化、下載器）。
*   **預期成果**：把胖嘟嘟的 `main.py` 瘦身到 50 行以內，讓它變成一個一眼就能看懂的「總指揮劇本」。

## 🛡️ 第二階段：防呆防禦線 (Robustness)
**目標：讓「例外處理」不再干擾核心邏輯。**
*   **自訂例外 (Custom Exceptions)**：告別籠統的 `try...except Exception`。針對業務邏輯建立如 `UserNotFoundError` 或 `ParsingElementError` 這種精準的類別。
*   **導入日誌系統 (Logging)**：全面拋棄粗糙的 `print()`。改用內建的 `logging` 模組，依照 `INFO`, `WARNING`, `ERROR` 區分層級，讓測試時不用再對著螢幕發呆。

## 🎯 第三階段：自動化安全網 (Automated Testing)
**目標：解決「改 A 壞 B」的重構恐懼症。**
*   **導入 Pytest**：學習與使用業界最流行的測試框架。
*   **撰寫單元測試 (Unit Test)**：針對拆出去的 `utils.py`，寫下「給予 A 輸入，預期得到 B 輸出」的嚴格防線。
*   **破壞性邊界測試**：專門餵給程式最爛的資料（空字串、負數、括號與特殊字元），檢驗防呆機制是否足夠強壯。
*   **預期成果**：未來每次修改程式，只要執行 `pytest` 指令，三秒內就能確認有沒有寫壞舊功能。

## 💎 第四階段：職業化標準 (Professional Standard)
**目標：寫出「看起來很專業」的程式碼。**
*   **遵循 PEP 8**：使用如 Black 這種自動格式化工具，讓程式碼風格像出自大廠工程師之手。
*   **強型別標記 (Type Hinting)**：全面加入 `def add(a: int) -> int:` 這種函數簽名，大幅強化 VS Code 的自動提示與防錯能力。
*   **Git 頻繁版控**：用 commit 和 branch 取代土炮的 `_v2_final.py` 命名法。

---

### 💡 反思
這是一個經典的蛻變過程。如果說前幾天我們寫的是一台為了達成任務而硬生生拼出來的「能動的大卡車」，那現在我們就是要把它送回原廠，重新設計出獨立的引擎控制模組 (Utils)、碰撞感測器 (Exception Logging) 與自動檢測儀器 (Pytest)，將它打造成一台商業級的裝甲車！

**下一站：正式動刀，開啟目錄拆分！**
